# @bitcode/generic-artifacts-vercel

**Vercel Blob** `ArtifactStorage` provider base.

```
@bitcode/artifact-generics  ArtifactStorage contract
        ↑
@bitcode/generic-artifacts-vercel   # this package
```

Env: `BLOB_READ_WRITE_TOKEN` (or `ARTIFACT_VERCEL_BLOB_TOKEN`).

# @bitcode/generic-artifacts-aws

**AWS S3** `ArtifactStorage` provider base.

```
@bitcode/artifact-generics  ArtifactStorage contract
        ↑
@bitcode/generic-artifacts-aws   # this package (S3)
```

Env: `ARTIFACT_S3_BUCKET`, `AWS_REGION` (+ standard AWS credentials).

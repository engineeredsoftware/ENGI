# generic-mcps (relocated)

The Exchange-facing Bitcode MCP server moved to **`apps/mcp`**
(`@bitcode/generic-mcps-bitcode`).

Shared MCP generics (if any) remain under packages; runnable MCP apps live in
`apps/`.

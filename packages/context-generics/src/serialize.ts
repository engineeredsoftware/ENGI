/**
 * BC serialize surface — prefer serializeProcessRootFields.
 */

export {
  serializeContext,
  serializeProcessRootFields,
  type GlobalContext,
  type ProcessRootFields,
} from '@bitcode/generic-executions';

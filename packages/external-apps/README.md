# external-apps (relocated)

Runnable external-agent surfaces moved to **`apps/`**:

| Former path | New path |
|-------------|----------|
| `packages/external-apps/chatgpt` | `apps/chatgpt` |
| `packages/external-apps/claude` | `apps/claude` |

This directory is retained as a pointer only.

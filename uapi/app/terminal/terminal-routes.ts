/**
 * @deprecated Compatibility shim. Import from
 * `@/components/bitcode/routes/product-routes` instead.
 * TERMINAL_ROUTE now aliases PACKS_ROUTE (Terminal eradicated).
 */
export {
  PACKS_ROUTE,
  READS_ROUTE,
  DEPOSITS_ROUTE,
  READ_ROUTE,
  DEPOSIT_ROUTE,
  EXCHANGE_ROUTE,
  TERMINAL_ROUTE,
  buildPacksHref,
  buildReadsHref,
  buildReadHref,
  buildDepositsHref,
  buildDepositHref,
  buildExchangeHref,
  buildTerminalHref,
} from '@/components/bitcode/routes/product-routes';
